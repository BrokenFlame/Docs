apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::********:role/app-role
  name: app-secret-store
  namespace: default
spec:
  provider:
    aws:
      auth:
        jwt:
          serviceAccountRef:
            name: sa-app
      region: eu-west-2
      service: SecretsManager
